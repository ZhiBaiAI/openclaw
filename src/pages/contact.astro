---
import MarketingLayout from '../layouts/MarketingLayout.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const withBase = (path: string) => `${base}${path}`;
---

<MarketingLayout
  title="OpenClaw 交付支持"
  description="把未交付客户的沟通方式和已交付客户的支持路径分开说明，便于长期使用。"
>
  <main class="container">
    <section class="hero split-hero">
      <div class="hero-grid">
        <article class="hero-copy">
          <span class="eyebrow">交付支持</span>
          <h1>未交付客户先沟通需求，已交付客户优先走文档和问题排查。</h1>
          <p class="lead">
            先找对入口，再沟通，会快很多。
          </p>
        </article>

        <aside class="panel">
          <h2>建议先准备这些信息</h2>
          <ul class="bullet-list">
            <li>你的设备类型和系统版本</li>
            <li>你想先跑通的 1 到 2 个实际场景</li>
            <li>当前已经接入或准备接入的聊天工具</li>
            <li>如果有问题，附上报错现象、截图和日志</li>
          </ul>
        </aside>
      </div>
    </section>

    <section class="section">
      <div class="card-grid">
        <article class="section-card">
          <span class="eyebrow">未交付客户</span>
          <h2>先发需求，不要先发一堆环境细节。</h2>
          <ul>
            <li>先说明你想解决什么问题，而不是先贴一堆命令输出</li>
            <li>告诉我设备类型、常用聊天入口和最希望先跑通的场景</li>
            <li>通过你当前已经建立的沟通渠道发起预约即可</li>
          </ul>
        </article>

        <article class="section-card">
          <span class="eyebrow">已交付客户</span>
          <h2>先自查一轮，再发问题会更快定位。</h2>
          <ul>
            <li>先看 7 步文档、命令速查和 FAQ</li>
            <li>再附上你的设备、现象、截图和日志</li>
            <li>如果是接入、提醒、规则类问题，尽量描述“预期是什么、现在发生了什么”</li>
          </ul>
        </article>

        <article class="section-card">
          <span class="eyebrow">推荐查看顺序</span>
          <h2>先查文档，再带着问题来沟通。</h2>
          <ul>
            <li>先看 <a href={withBase('/docs/')}>使用文档</a></li>
            <li>再看 <a href={withBase('/docs/reference/common-commands/')}>常用命令速查</a></li>
            <li>补充看 <a href={withBase('/links/')}>资源导航</a> 和 <a href={withBase('/cases/')}>真实案例</a></li>
          </ul>
        </article>
      </div>
    </section>

    <section class="section contact-qr-section">
      <div class="spotlight">
        <article class="section-card qr-card">
          <span class="eyebrow">扫码联系</span>
          <h2>需要预约交付或继续沟通，直接扫码就行。</h2>
          <p>适合预约安装、补充需求、反馈使用问题。</p>
        </article>

        <article class="section-card qr-image-card">
          <img src={withBase('/wechat-qr.png')} alt="微信联系二维码" />
          <p>微信扫码添加，备注你的设备类型或问题场景，会更容易快速对上。</p>
        </article>
      </div>
    </section>

    <footer class="footer">
      常见问题先自查，仍未解决时再通过现有沟通渠道联系。
    </footer>
  </main>

  <style>
    .contact-qr-section {
      padding-top: 0;
    }

    .qr-card p,
    .qr-image-card p {
      margin: 14px 0 0;
      color: var(--muted);
      line-height: 1.75;
    }

    .qr-image-card {
      justify-items: center;
      text-align: center;
      gap: 0;
    }

    .qr-image-card img {
      width: min(100%, 220px);
      border-radius: 24px;
      border: 1px solid rgba(217, 94, 57, 0.12);
      background: rgba(255, 252, 247, 0.96);
      padding: 12px;
      box-shadow: 0 18px 34px rgba(127, 79, 47, 0.08);
    }
  </style>
</MarketingLayout>
